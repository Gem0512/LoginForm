import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css'; // import CSS vào đây

import Login from './LoginForm';
// import { ToastContainer } from 'react-toastify';

ReactDOM.render(
  <React.StrictMode>
    <Login/>  
  </React.StrictMode>,
  document.getElementById('root')
);
